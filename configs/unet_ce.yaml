experiment_id: E4
question: Does the Dice component improve validation mIoU over weighted cross-entropy alone?
dataset_manifest: artifacts/dataset-manifest-v1.json
model: unet
in_channels: 4
num_classes: 6
base_channels: 16
loss: weighted_ce
optimizer: adamw
learning_rate: 0.001
weight_decay: 0.0001
batch_size: 12
epochs: 12
patience: 4
seed: 42
device: auto
num_workers: 0
augmentation: dihedral
mlflow_experiment: finland-geospatial-ai
output: artifacts/checkpoints/unet-ce-best.pt
